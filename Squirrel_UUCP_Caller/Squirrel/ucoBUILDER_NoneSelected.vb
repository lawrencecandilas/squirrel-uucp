Public Class ucoBUILDER_NoneSelected
    'This isn't a real builder, so it's missing a lot.
    Public Modified As Boolean = False

    Private Sub ucoBUILDER_NoneSelected_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        Dock = DockStyle.Fill
    End Sub

End Class
